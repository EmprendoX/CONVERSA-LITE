TaskMaster Rules – ConversaX Lite
🎯 Principio General
Todas las tareas, código y decisiones deben cumplir con el PRD – ConversaX Lite.
Nada fuera de este PRD puede implementarse sin autorización explícita.
El sistema debe ser rápido de desplegar, simple de mantener y orientado a un solo cliente.

1) Stack Autorizado
	• Backend: Node.js + Express
	• Frontend (opcional): React + Vite + Tailwind (solo Leads & Items Panel)
	• DB: Supabase (Postgres)
	• Vector DB: Chroma (default) o Pinecone (si remoto)
	• IA: OpenAI (text-embedding-3-small y gpt-4o-mini o gpt-4.1)
	• Canal Mensajería: WhatsApp Cloud API (Twilio solo fallback)
	• Seguridad: JWT, HMAC (X-Hub-Signature-256), .env obligatorio
⚠️ Prohibido:
	• Agregar frameworks no mencionados.
	• Usar servicios externos no especificados.
	• Inventar librerías o dependencias “raras”.

2) Reglas de Arquitectura
	• src/api → solo routers (whatsapp.ts, chat.ts, items.ts, tools.ts).
	• src/lib → lógica de negocio (rag.ts, intents.ts, validators.ts, meta.ts).
	• app.ts → inicialización única, sin lógica de negocio.
	• Cada endpoint debe:
		○ Validar inputs.
		○ Manejar errores sin romper el flujo (fallback seguro).
		○ Loggear en analytics_events.

3) Reglas de Seguridad
	1. Nunca exponer secretos en logs, commits ni respuestas.
	2. Siempre validar firma HMAC en /api/whatsapp.
	3. Idempotencia obligatoria: todos los message.id deben ser únicos.
	4. Logs seguros: datos PII mínimos, cifrado en reposo si aplica.
	5. Errores críticos: deben resolverse con fallback → “Gracias, ya te ayudo con eso…” en lugar de romper el chat.

4) Reglas de RAG
	• Indexar catálogo en embeddings con text-embedding-3-small.
	• retrieveContext() debe devolver al menos topK=3 snippets.
	• El bot solo responde con datos del catálogo + historial de sesión.
	• Prohibido inventar precios, stock o políticas.
	• Si falta información → el bot debe preguntar.

5) Reglas de Intenciones & 
	• Cada respuesta debe terminar con un bloque <ANALYTICS>...</ANALYTICS> válido.
	• Si falta o es inválido → reintentar validación 1 vez.
	• Si sigue fallando → fallback genérico + log de error.
	• Motor de acciones:
		○ BUY_INTENT_FIRM → CREATE_LEAD + send_payment_link
		○ APPOINTMENT_SCHEDULING → SCHEDULE_CALL
		○ SHIPPING_QUOTE → pedir CP si falta → SEND_SHIPPING_QUOTE
		○ PURCHASE_OBJECTION → manejo objeción + SEND_CATALOG
		○ ORDER_STATUS → SEND_TRACKING o ESCALATE
Escalar inmediatamente: queja legal, VIP, reembolso alto, negativo repetido.

6) Reglas de Endpoints
	• /api/whatsapp
		○ GET → verify token.
		○ POST → validar firma, idempotencia, llamar handleInboundMessage().
	• /api/chat
		○ POST → input {message, sessionId}.
		○ Llama retrieveContext + callLLM + validateAnalytics.
	• /api/items
		○ CRUD básico (title, desc, price, url).
	• /api/tools
		○ Acciones seguras (send_payment_link, schedule_call, send_catalog_snippet).
	• Todos los endpoints deben devolver 200 OK rápido a WhatsApp (aunque internamente haya error).

7) Reglas de Tests
	• Unit obligatorio: validadores (validateAnalytics, HMAC).
	• Integración: /api/whatsapp → /api/chat → /api/tools.
	• E2E: mensaje real desde tester → lead en Supabase.
	• Casos de error: firma inválida, 24h cerrada, rate limit LLM.

8) Reglas de Observabilidad
	• correlation_id en todos los eventos.
	• Alertar si:
			§ 5% errores de firma HMAC.
		○ p95 > 3s en /api/chat.
	• Fallback obligatorio si LLM falla o está en outage.

9) Reglas de Lanzamiento
Antes de entregar al cliente:
	• Webhook verificado (200 OK).
	• Catálogo cargado + embeddings creados.
	• Prompt de sistema probado con 3 few-shots.
	• Buttons/List funcionando.
	• Lead guardado en Supabase.
	• Link de pago probado.
	• Video corto de E2E (prueba real).
	• Documentación mínima para cliente (acceso + credenciales).

10) Reglas de Blindaje
	• Nunca mezclar features de SaaS multi-tenant aquí.
	• Nunca agregar endpoints de billing o onboarding masivo.
	• Nunca salir del alcance de este PRD.
	• Siempre basarse en el catálogo + taxonomía de intenciones V2.
	• Siempre priorizar simplicidad, despliegue rápido y mantenimiento mínimo.
	
11) 🚨 Reglas de Base de Datos – Supabase
	1. Uniformidad absoluta: todo el código debe basarse únicamente en las tablas y columnas ya definidas en Supabase para este proyecto.
	2. Prohibido renombrar, eliminar o inventar columnas/tablas.
	3. Consistencia: el código, migraciones y queries deben usar siempre los mismos nombres y tipos que ya existen en la base.
	4. Extensión controlada: si se requiere un nuevo campo, debe agregarse como extensión documentada, nunca alterando ni rompiendo lo ya creado.
	5. Validación previa: antes de generar código que interactúe con la base, confirmar que la tabla y columna usada existe en Supabase; si no, loggear error y usar fallback seguro.
Snake_case obligatorio: nunca transformar a camelCase ni otros formatos.